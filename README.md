# Vitest Demo



playwright doesn't run in stackblitz, unit tests run fine but not catching console.logs

for stackblitz:
- @vitejs/plugin-react pinned to ^5.2.0
- vite pinned to ^7


Run `pnpm test:run` to see console output
